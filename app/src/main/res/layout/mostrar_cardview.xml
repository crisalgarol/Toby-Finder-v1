<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ly_cardview"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:orientation="vertical"
    tools:context="com.example.bekoman.toby_main.mostrar_cardview"
    android:background="@drawable/m56">

    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id = "@+id/reciclador_rv"/>

    <TextView
        android:text="No se han encontrado resultados, ¿seguro que lo escribiste correctamente?"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/no_results"
        android:textAlignment="center"
        android:textSize="30sp"
        android:textColor="@color/cardview_light_background"
        android:background="@color/primary_dark"
        android:alpha="80"
        android:gravity="center_vertical|center_horizontal" />

</LinearLayout>
